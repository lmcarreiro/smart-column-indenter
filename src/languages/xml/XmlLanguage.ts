import Scanner from "./base/Scanner";
import Token, { TokenType } from "../Token";

export default class XmlScanner extends Scanner {
    public scan(code: string): Token[]
    {
        throw new Error("Not implemented yet");
    }
}
